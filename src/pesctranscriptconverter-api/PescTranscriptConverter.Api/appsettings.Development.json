{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "GotenbergSharpClient": {
    "ServiceUrl": "http://localhost:3000",
    "HealthCheckUrl": "http://localhost:3000/health"
  },
  "CdlAssets": {
    "RootDirectory": "../../../assets/cdl" // This is because aspire runs in the project directory, not the bin directory
  },
  "PdfAssets": {
    "RootDirectory": "../../../assets/pdf" // This is because aspire runs in the project directory, not the bin directory
  }
}
